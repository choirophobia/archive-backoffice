DATABASE_URL=postgresql://slo_user:slo_pass@localhost:5432/slo_archive
JWT_SECRET=changeme
PORT=4000
# Optional: directory where per-record PDF attachments are stored on disk.
# Defaults to backend/storage/pdfs.
PDF_STORAGE_DIR=
